import 'package:flutter/material.dart';

class HomeworkItemWidget extends StatelessWidget {
  const HomeworkItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
